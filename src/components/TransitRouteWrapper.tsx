import React from 'react';
import {
  useQueryClient,
  QueryClient,
  QueryClientProvider,
} from 'react-query';
import TransitService from '../services/TransitService';

export const TransitRouteWrapper = () : JSX.Element  => {
  return(
    <>
      <h2>Hehe some stuff</h2>
    </>
  );
};
